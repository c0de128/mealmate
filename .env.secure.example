# SECURITY NOTICE: 
# NEVER commit .env files to version control
# ALWAYS regenerate API keys if they are exposed

# Database Configuration
# PostgreSQL connection string (required for production)
# Example for Neon: postgresql://username:password@hostname/database?sslmode=require
# Example for local PostgreSQL: postgresql://username:password@localhost:5432/mealmate
DATABASE_URL="postgresql://username:password@hostname/database"

# AI Configuration
# Mistral AI API key for recipe parsing functionality
# Get your API key from: https://console.mistral.ai/
# IMPORTANT: Keep this key secret and never commit it to version control
MISTRAL_API_KEY="your_mistral_api_key_here"

# Server Configuration
# Port for the server to listen on (optional, defaults to 5000)
PORT=5000

# Environment
# Set to 'development' for dev mode, 'production' for production
NODE_ENV=development

# Security Best Practices:
# 1. Store API keys in environment variables only
# 2. Use a password manager or secure vault for production keys
# 3. Rotate keys regularly
# 4. Never share API keys in code, comments, or documentation
# 5. Add .env to .gitignore immediately